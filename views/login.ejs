<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/styleLogin.css">
</head>
<body>
    <div style="justify-content: center;align-items: center;text-align: center;">
        <a id="aktivitar" href="/" style="text-decoration: none; color: white; justify-content: center;">AKTIVITÁŘ</a>
    </div>

    <div class="background">
        <div class="content">
            
            <p id="logintext">PŘIHLÁŠENÍ</p>
           
            
            <form class= "login" id = "login" action="/login" method="post">
                <input class="textfield" name="loginMail" type="email" placeholder="Email" required/>
                <input class="textfield" name="loginPass" type="password" placeholder="Heslo" required/>

                <div>
                    <% if (errors && errors.length > 0) { %>
                        <% if(errors=="Registrace byla úspěšná"){ %>
                            <div class="popup-success"><%= errors %> </div> 
                        <% }else{ errors.forEach((error) => { %>
                            <div class="popup-error">
                                <%= error %>
                            </div>
                        <% }); %>
                        <%}%>
                    <% } %>
                </div>

                <div class="registertext">Nejste registrovaní? <a href="/register">Registrovat se</a></div>
                <button class="submit" type="submit">Přihlásit se</button>
              
            </form>
        </div>
    </div> 
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
